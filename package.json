{
  "name": "globalize-config-loader",
  "version": "0.1.2",
  "description": "Webpack loader for globalize-config-compiler.",
  "main": "lib/loader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "globalize-config-compiler": "^0.1.2",
    "loader-utils": "^1.1.0"
  },
  "peerDependencies": {
  },
  "keywords": [
    "webpack",
    "loader"
  ]
}
